apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../../base  # This references a base config (we'll create it next if missing)
images:
  - name: ghcr.io/ericvalijani/bash-mastery-devops  # Your image repo (adjust if different)
    newTag: 194fcc4
