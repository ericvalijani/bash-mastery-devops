#!/bin/sh
#!/bin/sh
# Smoke test – we just want to know the container can start and run a command
echo "healthcheck: container is alive and bash works"
exit 0
#curl -f http://localhost:8080/health || exit 1

